"""Framework adapters for vecr-compress.

Each adapter is optional and lazily imports its framework dependency. Install
the right extra to use them:

    pip install vecr-compress[langchain]
    pip install vecr-compress[llamaindex]
"""
